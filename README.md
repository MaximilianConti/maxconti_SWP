# maxconti_SWP

Ich verstehe den objektorientierten bruchaddierer nochh nicht ganz.

ERKLÄRUNG:

static = static macht eine Methode zur Klassenmethode (nicht Objektbezogen).

return `${gekZ}/${gekN}` = `${}` ist String Interpolation in JS.  also eif nur wie "bla bla + name +"

 new = Schlüsselwort zum Erzeugen einer neuen Instanz eines Objekts/Klasse. wie let nur für klassen und objekte

 this =  Schlüsselwort, dass auf das aktuelle Objekt verweist. eif wie in funktion bla= nur halt davor new wenn man was neues in der klasse erstellt

 
